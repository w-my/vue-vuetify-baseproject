import Vue from 'vue';
import Vuetify from 'vuetify/lib/framework';

Vue.use(Vuetify);

export default new Vuetify({
  theme: {
    themes: {
      light: {
        // primary: '#F57C00',
        // secondary: '#FB8C00',
        // accent: '#B39DDB',
        // info: '#2196F3',
        // error: '#FF5252',
        // success: '#4CAF50',
        // warning: '#FFC107',
      }
    }
  }
});
